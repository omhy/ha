### TIMERS
- platform: history_stats
  name: computer_on_today
  entity_id: binary_sensor.ping_computer
  type: "time"
  state: "on"
  start: "{{ now().replace(hour=0).replace(minute=0).replace(second=0) }}"
  end: "{{ now() }}"

- platform: history_stats
  name: laptop_on_today
  entity_id: binary_sensor.ping_laptop
  type: "time"
  state: "on"
  start: "{{ now().replace(hour=0).replace(minute=0).replace(second=0) }}"
  end: "{{ now() }}"

- platform: history_stats
  name: tv_on_today
  entity_id: binary_sensor.ping_tv
  type: "time"
  state: "on"
  start: "{{ now().replace(hour=0).replace(minute=0).replace(second=0) }}"
  end: "{{ now() }}"

- platform: history_stats
  name: dima_detected_activity_on_bicycle
  entity_id: sensor.sm_g973f_detected_activity
  type: "time"
  state: "on_bicycle"
  start: "{{ now().replace(hour=0).replace(minute=0).replace(second=0) }}"
  end: "{{ now() }}"

- platform: history_stats
  name: dima_detected_activity_still
  entity_id: sensor.sm_g973f_detected_activity
  type: "time"
  state: "still"
  start: "{{ now().replace(hour=0).replace(minute=0).replace(second=0) }}"
  end: "{{ now() }}"

- platform: history_stats
  name: dima_detected_activity_in_vehicle
  entity_id: sensor.sm_g973f_detected_activity
  type: "time"
  state: "in_vehicle"
  start: "{{ now().replace(hour=0).replace(minute=0).replace(second=0) }}"
  end: "{{ now() }}"

- platform: history_stats
  name: dima_detected_activity_on_foot
  entity_id: sensor.sm_g973f_detected_activity
  type: "time"
  state: "on_foot"
  start: "{{ now().replace(hour=0).replace(minute=0).replace(second=0) }}"
  end: "{{ now() }}"

- platform: history_stats
  name: dima_detected_activity_running
  entity_id: sensor.sm_g973f_detected_activity
  type: "time"
  state: "running"
  start: "{{ now().replace(hour=0).replace(minute=0).replace(second=0) }}"
  end: "{{ now() }}"

- platform: history_stats
  name: dima_detected_activity_walking
  entity_id: sensor.sm_g973f_detected_activity
  type: "time"
  state: "walking"
  start: "{{ now().replace(hour=0).replace(minute=0).replace(second=0) }}"
  end: "{{ now() }}"

- platform: history_stats
  name: dima_detected_activity_tilting
  entity_id: sensor.sm_g973f_detected_activity
  type: "time"
  state: "tilting"
  start: "{{ now().replace(hour=0).replace(minute=0).replace(second=0) }}"
  end: "{{ now() }}"

- platform: history_stats
  name: dima_detected_activity_unknown
  entity_id: sensor.sm_g973f_detected_activity
  type: "time"
  state: "unknown"
  start: "{{ now().replace(hour=0).replace(minute=0).replace(second=0) }}"
  end: "{{ now() }}"

- platform: history_stats
  name: dima_charger_type_ac
  entity_id: sensor.sm_g973f_charger_type
  type: "time"
  state: "ac"
  start: "{{ now().replace(hour=0).replace(minute=0).replace(second=0) }}"
  end: "{{ now() }}"

- platform: history_stats
  name: dima_charger_type_usb
  entity_id: sensor.sm_g973f_charger_type
  type: "time"
  state: "usb"
  start: "{{ now().replace(hour=0).replace(minute=0).replace(second=0) }}"
  end: "{{ now() }}"

- platform: history_stats
  name: dima_charger_type_wireless
  entity_id: sensor.sm_g973f_charger_type
  type: "time"
  state: "wireless"
  start: "{{ now().replace(hour=0).replace(minute=0).replace(second=0) }}"
  end: "{{ now() }}"

- platform: history_stats
  name: dima_interactive
  entity_id: binary_sensor.sm_g973f_interactive
  type: "time"
  state: "on"
  start: "{{ now().replace(hour=0).replace(minute=0).replace(second=0) }}"
  end: "{{ now() }}"

- platform: history_stats
  name: yury_interactive
  entity_id: binary_sensor.redmi_note_9_pro_interactive
  type: "time"
  state: "on"
  start: "{{ now().replace(hour=0).replace(minute=0).replace(second=0) }}"
  end: "{{ now() }}"

- platform: history_stats
  name: alyona_interactive
  entity_id: binary_sensor.mi_a2_interactive
  type: "time"
  state: "on"
  start: "{{ now().replace(hour=0).replace(minute=0).replace(second=0) }}"
  end: "{{ now() }}"
