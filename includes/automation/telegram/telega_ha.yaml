- alias: "ha_start"
  trigger:
    - platform: homeassistant
      event: start
  action:
    - service: script.state_change_update_ha_on
    - service: telegram_bot.send_message
      data_template:
        target: !secret tg_chat_dima
        message: "{{ now().strftime('%H:%M') }} \U0001F916 {{ states.sensor.version_ha_current.state }} #HA is back online"
        inline_keyboard:
        - "\U0001F532 Menu:/menu, \U0000267B Dismiss:/dismiss"

- alias: "ha_shutdown"
  trigger:
    - platform: homeassistant
      event: shutdown
  action:
    - service: script.state_change_update_ha_off
    - service: telegram_bot.send_message
      data_template:
        target: !secret tg_chat_dima
        message: "{{ now().strftime('%H:%M') }} \U0001F6D1 {{ states.sensor.version_ha_current.state }} #HA goes offline (uptime {{ (1440 * states.sensor.uptime.state | float) | round(0)}} min)"
        inline_keyboard:
          - "\U0000267B Dismiss:/dismiss"

- alias: "ha_bad_login"
  trigger:
    - platform: state
      entity_id: persistent_notification.httplogin
  condition:
    - condition: template
      value_template: "{{ trigger.to_state.state == 'notifying' }}"
  action:
    - service: telegram_bot.send_message
      data_template:
        target: !secret tg_chat_dima
        message: "{{ now().strftime('%H:%M') }} \U00002620 {{ states.persistent_notification.httplogin.attributes.message }}"
        inline_keyboard:
        - "\U0000267B Dismiss:/dismiss"
    - service: persistent_notification.dismiss
      data:
        notification_id: "httplogin"