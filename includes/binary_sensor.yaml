#- platform: template
#  sensors:
#    computer_is_on:
#      value_template: "{{states.sensor.computer_probe.state == 'True'}}"
#    laptop_is_on:
#      value_template: "{{states.sensor.laptop_probe.state == 'True'}}"
